export interface GenerateCodeOptions {
  output: string;
  includeRelationFields: boolean;
  includeRelationFromFields: boolean;
  dtoSuffix: string;
  enumSuffix: string;
  dtoPrefix: string;
  enumPrefix: string;
}
